# Schema

key_1: list(str(max=3))
key_2: map(num())
key_3: map(include('toto'), required=False)
key_4: map(include('very_deep'), required=False)
functions: map(str())
taratata: include('very_deep', required=False)

complexKey:
  k1: int()
  k2: num()
  k3: str()
  k4: include('toto', required=False)
  k5: include('very_deep', required=False)
  k6:
    k7: str()

foo: include('bar')


---
bar:
  i1: str()
  i2: str()
  i3: include('very_deep', required=False)

toto:
  x: int()
  y: str()
  z: include('very_deep', required=False)

non_used:
  a: num()

---
very_deep:
  t: num()

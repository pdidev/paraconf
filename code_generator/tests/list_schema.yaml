key1: list(int())
key2: list(list(str()))

key3:
  k1: list(num(), required=False)
  k2:
    k3: list(str(), int(), list(num()))

key4: list(int(), list(str(), int(), num(), required=False), required=False)


# To access an hypothetic 5th value of key4, and of type list(list(str())):
# root->key4->tab[5].value1->tab[indice].value2
#
# root->key4->type               ---> enum {ROOT_KEY4_LONG, ROOT_KEY4_LIST0}
# root->key4->tab[i].value->type ---> enum {ROOT_KEY4_LONG, ROOT_KEY4_STR, ROOT_KEY4_DOUBLE}
